{
  "name": "pd-truck",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "export": "next build && next export",
    "deploy": "npm run export && npx gh-pages -d out"
  },
  "dependencies": {
    "next": "13.2.1",
    "next-images": "^1.8.4",
    "next-remote-watch": "^2.0.0",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
